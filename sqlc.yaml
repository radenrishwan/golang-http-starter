version: "2"
sql:
  - engine: "postgresql"
    queries: "migrations/queries"
    schema: "migrations/schema"
    gen:
      go:
        package: "query"
        out: "migrations/query"
        sql_package: "pgx/v5"
